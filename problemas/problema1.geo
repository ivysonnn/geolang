// Problema 1: avalia x^2 - y + c, com x e y racionais e c inteiro,
// armazenados previamente no codigo-fonte, e imprime o resultado.
fn main() {
    x := 3.5;
    y := 2.0;
    c := 4;

    var resultado: float = x * x - y + c;

    io.print(resultado);
}
